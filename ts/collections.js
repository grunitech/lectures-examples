const fruits = ['apple', 'orange', 'cherry'];
fruits.push('false');
const listOfAnyValue = [];
const listOfAny = listOfAnyValue[1];
listOfAny.getDate();
const anUnknownItemsList = [];
const anUnknownItem = anUnknownItemsList[1];
anUnknownItem.getDate();
const stringOrNumbers = [1, 'a', 2, 'b'];
const myPoint = [1.1, 5.3];
const spaceTime = [2.2, 5, 7, 12, 'pm'];
function calcSpaceTime(sp) {
    console.log(sp);
}
const mySpaceTime = [1, 2, 3, 4, ''];
calcSpaceTime([1, 2, 3, 4, '']);
function verify(token) {
    return false;
}
const myToken = 'any string';
